import { StyleSheet } from 'react-native';
import { verticalScale } from 'react-native-size-matters';
import { GRAY_MEDIUM, GRAY_TEXT, PRIMARY, WHITE } from '../../styles/colors';
import { margin, WINDOW_HEIGHT, WINDOW_WIDTH, padding } from '../../styles/mixins';
import { FONT_BOLD, FONT_REGULAR, FONT_SEMI_BOLD, FONT_SIZE_18, SMALL_TEXT, FONT_SIZE_20 } from '../../styles/typography';

export default StyleSheet.create({
   
})
